'use client';
import Link from 'next/link';import{useState}from'react';import{Menu,X,ArrowRight}from'lucide-react';import{navigation,footerGroups}from'@/data/navigation';import{site}from'@/config/site';
export function Wordmark({inverse=false}:{inverse?:boolean}){return <Link href="/" className="wordmark" aria-label="IRRIVO home"><strong style={{color:inverse?'#fff':'var(--red)'}}>IRRIVO</strong><small style={{color:inverse?'#fff':'var(--ink)'}}>by <span>SHD</span></small></Link>}
export function Header(){const[open,setOpen]=useState(false);return <header className="site-header"><div className="container nav-wrap"><Wordmark/><nav aria-label="Main navigation" className={open?'nav-links open':'nav-links'}>{navigation.map(n=><Link key={n.label} href={n.href} onClick={()=>setOpen(false)}>{n.label}</Link>)}<div className="mobile-actions"><Link href={site.cta.signIn} className="btn btn-secondary">Sign In</Link><Link href={site.cta.start} className="btn btn-primary">Get Started</Link></div></nav><div className="desktop-actions"><Link href={site.cta.signIn} className="btn btn-secondary">Sign In</Link><Link href={site.cta.start} className="btn btn-primary">Get Started</Link></div><button className="menu" aria-label="Toggle navigation" aria-expanded={open} onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></header>}
export function Footer(){return <footer className="footer"><div className="container"><div className="footer-top"><div><Wordmark inverse/><p>Workforce operations, made clear.</p></div>{footerGroups.map(g=><div key={g.title}><h3>{g.title}</h3>{g.links.map(([l,h])=><Link key={l} href={h}>{l}</Link>)}</div>)}</div><div className="footer-bottom"><span>© {new Date().getFullYear()} IRRIVO. All rights reserved.</span><span>Built by {site.legalName} in Ghana.</span></div></div></footer>}
export function CTA(){return <section className="cta"><div className="container cta-inner"><div><span className="eyebrow">Move with clarity</span><h2>See your workforce clearly.</h2><p>Bring scheduling, attendance, location awareness and workforce intelligence into one system.</p></div><div className="cta-buttons"><Link className="btn btn-primary" href={site.cta.start}>Get Started <ArrowRight size={17}/></Link><Link className="btn btn-secondary" href={site.cta.demo}>Book a Demo</Link></div></div></section>}
