import Link from'next/link';import{ArrowRight,Check,LockKeyhole,MapPin,Network,Users}from'lucide-react';import type{PageData}from'@/data/pages';import{CTA}from'./site-shell';import{AnalyticsChart,AskShadyChat,RosterGrid}from'./product-visuals';import'./page-template.css';import'./product-visuals.css';
export function MarketingPage({data,visual='network'}:{data:PageData;visual?:'network'|'roster'|'analytics'|'ai'}){return <main id="main"><section className="subhero"><div className="container grid-2 align"><div><span className="eyebrow">{data.eyebrow}</span><h1 className="display">{data.title}</h1><p className="lead">{data.intro}</p><div className="sub-actions"><Link className="btn btn-primary" href="/contact?request=demo">Book a Demo <ArrowRight size={17}/></Link><Link className="btn btn-secondary" href="/pricing">View Pricing</Link></div></div><HeroVisual type={visual}/></div></section>{data.questions&&<section className="question-band dark-section"><div className="container"><span className="eyebrow">Try asking</span><div>{data.questions.map(q=><span key={q}>{q}</span>)}</div></div></section>}<section className="section"><div className="container"><div className="page-intro"><span className="eyebrow">What IRRIVO gives you</span><h2 className="h2">Built for clarity at every level.</h2></div><div className="grid-3 detail-grid">{data.points.map((p,i)=><article className="card" key={p.title}><span>0{i+1}</span><h3>{p.title}</h3><p>{p.text}</p></article>)}</div></div></section><section className="section soft"><div className="container grid-2 align"><div><span className="eyebrow">Connected operations</span><h2 className="h2">From the schedule to the live workforce.</h2><p className="lead">IRRIVO follows the effective expected work period, including approved roster shifts and accepted employee swaps, then connects it to attendance and operational reporting.</p></div><div className="flow-card card">{[['Plan',Network],['Confirm',MapPin],['Understand',Users]].map(([x,I],i)=><div key={String(x)}><I/><b>{String(x)}</b>{i<2&&<ArrowRight/>}</div>)}</div></div></section><section className="section security-mini dark-section"><div className="container grid-2 align"><div><LockKeyhole/><h2 className="h2">Access that follows responsibility.</h2></div><p>Job title and application permission are separate. Organizations configure who can create, submit or approve rosters, manage people, view analytics and change settings.</p></div></section><CTA/></main>}
function HeroVisual({type}:{type:string}){if(type==='roster')return <div className="page-visual"><RosterGrid/></div>;if(type==='analytics')return <div className="page-visual"><AnalyticsChart/></div>;if(type==='ai')return <div className="page-visual ai"><AskShadyChat/></div>;return <div className="network-visual card"><span className="org">IRRIVO Organization</span>{['Division','Department','Unit','Employee'].map((x,i)=><div key={x} style={{width:`${90-i*12}%`}}><Check/><b>{x}</b><small>Configurable structure</small></div>)}</div>}
