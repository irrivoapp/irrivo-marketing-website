import type{Metadata}from'next';import{Check,Minus}from'lucide-react';import{pricing}from'@/data/content';import{CTA}from'@/components/site-shell';import'./styles.css';export const metadata:Metadata={title:'Pricing',description:'Flexible IRRIVO plans for individuals, growing organizations and complex enterprises.'};const rows=[['Personal schedule',1,1,1,1],['Organization workforce management',0,1,1,1],['Rostering and attendance',0,1,1,1],['Geofencing',0,1,1,1],['Ask Shady','Basic','Plan limits','Advanced','Custom'],['Workforce Health Score',0,0,1,1],['Executive Morning Brief',0,0,1,1],['Advanced permissions',0,0,0,1],['Multi-site support',0,0,1,1]];export default function Page(){return <main id="main"><section className="pricing-hero"><div className="container"><span className="eyebrow">Pricing</span><h1 className="display">Simple, flexible plans.</h1><p className="lead">Start with the right level of workforce clarity and scale as your organization grows. Prices can be added when store and regional billing are finalized.</p></div></section><section className="section"><div className="container plan-grid">{pricing.map((p,i)=><article className={'plan card '+(i===1?'popular':'')} key={p.name}>{i===1&&<span className="popular-tag">Most popular</span>}<h2>{p.name}</h2><p>{p.audience}</p><div className="price-status">{i===0?'Free':i===3?'Custom':'Pricing available soon'}</div><ul>{p.features.map(f=><li key={f}><Check/>{f}</li>)}</ul><a className={'btn '+(i===1?'btn-primary':'btn-secondary')} href="/contact">{p.cta}</a></article>)}</div></section><section className="section soft comparison"><div className="container"><span className="eyebrow">Compare plans</span><h2 className="h2">Choose the capability you need.</h2><div className="comparison-table"><div className="compare-row head"><b>Capability</b>{pricing.map(p=><b key={p.name}>{p.name}</b>)}</div>{rows.map(r=><div className="compare-row" key={String(r[0])}><span>{r[0]}</span>{r.slice(1).map((v,i)=><span key={i}>{v===1?<Check/>:v===0?<Minus/>:v}</span>)}</div>)}</div></div></section><section className="section"><div className="container"><div className="faq-head"><span className="eyebrow">Common questions</span><h2 className="h2">Plan with confidence.</h2></div><div className="faq-grid">{[['Can we change plans later?','Yes. The plan architecture is designed to let organizations grow into additional capability.'],['Are mobile purchases supported?','IRRIVO is preparing Apple App Store and Google Play billing for eligible subscriptions.'],['Does a plan change user permissions?','No. Organization roles and granular permissions remain separate from billing tiers.'],['What does Enterprise include?','Custom limits, implementation, integrations, multi-site controls and dedicated support can be configured.']].map(x=><details className="card" key={x[0]}><summary>{x[0]}</summary><p>{x[1]}</p></details>)}</div></div></section><CTA/></main>}
