import type{Metadata}from'next';import{ArrowRight}from'lucide-react';import{industries}from'@/data/content';import{CTA}from'@/components/site-shell';import'./styles.css';export const metadata:Metadata={title:'Industries',description:'IRRIVO adapts to healthcare, banking, education, construction and complex multi-site workforces.'};export default function Page(){return <main id="main"><section className="industry-hero"><div className="container"><span className="eyebrow">Industries</span><h1 className="display">Your structure. Your workforce. One clear view.</h1><p className="lead">IRRIVO adapts to departments, wards, branches, teams, sites, projects, stores and campuses without forcing every organization into the same shape.</p></div></section><section className="section"><div className="container industry-grid">{industries.map(x=><article className="card" key={x.title}><div className="industry-icon"><x.icon/></div><h2>{x.title}</h2><p>{x.text}. Coordinate schedules, attendance, workforce coverage and useful management insight.</p><a target="_top" href={['healthcare','banking','education','construction'].includes(x.slug)?'/industries/'+x.slug:'/contact?request=demo'}>Explore {x.title}<ArrowRight/></a></article>)}</div></section><section className="section soft"><div className="container hierarchy"><div><span className="eyebrow">Organization structure</span><h2 className="h2">Represent the organization you actually run.</h2><p className="lead">Create a structure that fits your operations. Divisions, departments, wards, branches, teams, sites, projects and campuses can all live inside one organization.</p></div><div className="hierarchy-visual">{['Organization','Division','Department','Unit','Employee'].map(x=><span key={x}>{x}</span>)}</div></div></section><CTA/></main>}
